library(shiny)

server <- function(input, output) {
  output$plot <- renderPlot({
    plot(rnorm(input$num), type=input$plotType)
  })
  output$plot2 <- renderPlot({
    plot(rnorm(input$num2), type=input$plotType2)
  })
  
  
}